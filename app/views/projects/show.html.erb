<p id="notice"><%= notice %></p>

<p>
  <h1 class="text-center"> <%= @project.title %> </h1>
</p>

<h3>
  <strong><%= @project.description %></strong>
</h3>

<p class="text-center"> <%= link_to 'Edit', edit_project_path(@project) %> | <%= link_to 'Back', projects_path %> </p>

